如果想要对单个对象和对组合对象的调用没有差别，那么组合模式十分有用。这样的结果，
有可能是因为组合对象本质上和局部对象相似，也有可能因为在环境中组合对象和局部对象特征相同。
因为组合模式是树型结构，所以对整体的操作会影响到局部。通过组合，局部对客户端而言是透明的。
对象树也可以方便地进行遍历。

另一方面，组合模式又依赖于其组成部分的简单性，随着我们引入复杂的规则，
组合模式的代码将会变得难以维护。
组合模式不能很好的在关系数据库中保存。却非常适合使用XML进行持久化工作。