之前创建了标记语言(未完成)用于测验。现在就来看看测验所需要的问题。
假设我们创建了Question类并给它添加了mark()方法。该方法用于处理用户提交的答案。
目前我们的Question类支持MarkLogic语言、直接匹配、正则表达式三种方式来处理答案。
如图11-3示。
可是现在有了新的需求，问题的类型变多了。有可能是基于文本的问题，也有可能是基于多媒体的问题。
如果继续沿用继承体系，如图11-4，则会出现2个问题:
1.体系中类太多
2.每个类都要实现mark()方法

※只要发现自己正不断地在继承树的各个分支中重复同一个算法(无论是通过子类还是通过重复条件语句)，
请将这些算法抽象成独立的类型。