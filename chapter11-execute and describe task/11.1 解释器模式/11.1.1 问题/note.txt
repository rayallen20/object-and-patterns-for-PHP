现在有一个系统，90%的用户只使用了30%同样的功能。
但是整个系统还要支持另外10%的高级用户。这就使我们系统中的接口十分的混乱和臃肿。
怎么解决呢？
我们通过给这些高级用户提供一种领域语言(DSL Domain Specific Language 领域特定语言)来扩展我们系统的功能。

我们是可以使用eval()方法来解决这个问题，但是这样有2个问题:
1.安全性
2.复杂性

但是如果我们设计一门迷你语言，这门语言支持变量、equals操作符和布尔逻辑(or和and)操作符的语言。
就可以解决上边说的安全性和复杂性的问题。