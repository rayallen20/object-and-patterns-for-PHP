回调的作用:
利用回调，可以在运行时将和组件的核心任务没有直接关系的功能插入到组件中。
有了组件回调，就赋予了其他人在你不知道的上下文中扩展你的代码的权利。


create_function的用法: 已使用 $anonymous = function(){}; 替代  见代码4.12和4.12-2
// $anonymousFunc = create_function('形参代码', '函数体代码');
// $anonymousFunc(实参);

is_callable(array($obj, 'funcName'))
即:检测$obj的'funcName'方法是否为合法的调用结构

回调不一定是匿名的。也可以使用函数名作为回调。见代码4.12-3

也可以让方法返回匿名函数。见代码4.12-4

从方法中返回的匿名函数可以引用在它的父作用域中声明的变量。
就如同匿名函数还记得它被创建时所在的作用域。 见代码4.12-5