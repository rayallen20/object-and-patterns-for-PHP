4.5 延迟静态绑定
self和static的区别:
    self:指代self所在的这行代码外边的类
    static:指代调用static所在的这行代码的那个类

官方语言:static:指代被调用的类
         self:指代包含类

详细注解见代码4.5

static还可以作为静态方法调用的标识符。见代码4.5.1。
注意:4.5.1的例子举的不好，static::funcName() 并不是只能调用类的静态方法，非静态方法也可以调用。