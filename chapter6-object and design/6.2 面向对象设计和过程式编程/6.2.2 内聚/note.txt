内聚:指模块内部各成分之间相关联的程度的度量。
理想情况下应该使各组件职责清晰、分工明确。
如果代码间的关联范围太广，则不利于维护:因为在修改某部分代码之后还要修改相关的代码。

在ParamHandler类这个例子中，我们可以看到write()和read()两个方法可以共享数据(通过$this->params)。
且一个类方法中的改变可以很容易地反映到另一个方法中。因此可以说ParamHandler类是高度内聚的。
而过程式的例子中，则是把相关过程分开，导致处理XML的代码在多个函数中出现。